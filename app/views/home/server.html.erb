<section class="max-w-5xl mx-auto py-16 px-4">
  <img src="<%= image_path('server.jpg') %>" alt="Server" class="w-full h-64 object-cover rounded-xl mb-8">
  <h1 class="text-4xl font-bold text-gray-800 mb-4">Server</h1>
  <p class="text-gray-600">Visualizza le location globali dei nostri server e scegli quella più vicina a te.</p>
</section>

<% visit = Visit.find_by(section: 'server') %>
  <% if visit %>
    <p class="text-sm text-gray-500 italic">Visite totali: <%= visit.count %></p>
  <% end %>