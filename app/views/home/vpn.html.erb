<section class="max-w-5xl mx-auto py-16 px-4">
  <img src="<%= image_path('vpn.jpg') %>" alt="VPN" class="w-full h-64 object-cover rounded-xl mb-8">
  <h1 class="text-4xl font-bold text-gray-800 mb-4">VPN</h1>
  <p class="text-gray-600 mb-6">Benvenuto nella sezione VPN. Qui potrai scoprire tutte le funzionalità e vantaggi della nostra rete privata sicura.</p>
  
  <% visit = Visit.find_by(section: 'vpn') %>
  <% if visit %>
    <p class="text-sm text-gray-500 italic">Visite totali: <%= visit.count %></p>
  <% end %>
</section>
